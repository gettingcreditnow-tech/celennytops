// TEMPORARY STUB for Task 12 (PayPal capture route).
// Task 13 implements the real email sending logic and should replace this
// file entirely — these are no-op placeholders so the capture route compiles
// and the capture flow doesn't throw while Task 13 hasn't landed yet.

export async function sendOrderConfirmationEmail(order: any): Promise<void> {
  // no-op stub — replaced by Task 13
}

export async function sendAdminNewOrderEmail(order: any): Promise<void> {
  // no-op stub — replaced by Task 13
}
